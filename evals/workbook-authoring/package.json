{
  "name": "workbook-authoring-spike",
  "private": true,
  "type": "module",
  "description": "Spike #64: authoring-agent keystone. Fixtures, validation and stability tooling; contract schema lives in @cormac/contract.",
  "scripts": {
    "validate": "tsx scripts/validate-contract.ts",
    "diff": "tsx scripts/diff-contracts.ts"
  },
  "dependencies": {
    "@cormac/contract": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
